import React from "react";
import { Container } from "./navSideBar.elements";

import FormAndItems from "./FormAndItems";

const SideBarMain = () => {
  return (
    <Container>
      <FormAndItems />
    </Container>
  );
};

export default SideBarMain;
