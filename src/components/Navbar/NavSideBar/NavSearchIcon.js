import React from "react";
import SearchIcon from "@material-ui/icons/Search";
import { SearchIconContainer } from "./navSideBar.elements";

const NavSearchIcon = () => {
  return (
    <SearchIconContainer>
      <SearchIcon />
    </SearchIconContainer>
  );
};

export default NavSearchIcon;
