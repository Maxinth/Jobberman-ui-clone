import styled from "styled-components";

const NavSideBarBackDrop = styled.div`
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.3);
  z-index: 200;
  cursor: pointer;
`;

export { NavSideBarBackDrop };
